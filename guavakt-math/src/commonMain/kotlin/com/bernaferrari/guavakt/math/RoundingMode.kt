package com.bernaferrari.guavakt.math

/** Guava-compatible rounding modes (mirrors java.math.RoundingMode names). */
enum class RoundingMode {
    UP,
    DOWN,
    CEILING,
    FLOOR,
    UNNECESSARY,
    HALF_UP,
    HALF_DOWN,
    HALF_EVEN,
}
