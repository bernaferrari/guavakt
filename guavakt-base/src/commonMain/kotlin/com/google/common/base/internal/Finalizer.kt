package dev.guavakt.base.internal

/** Guava Finalizer — background finalizer thread not started on KMP. */
object Finalizer {
    fun startFinalizer(finalizeableClass: Any, frq: Any, referenceQueue: Any?): Any? = null
    fun isRunning(): Boolean = false
}
